# SECURITY.md — 비상 매뉴얼

> 키·비밀번호 노출이 의심될 때 따르는 대응 절차.
> `CLAUDE.md`의 "🚨 비상시 — 키 노출 의심" 규칙이 이 문서를 가리킨다.
> 빈칸(`[ ]`)은 실제 서비스 정보로 채워 넣으세요.

---

## 🚨 공통 대응 순서 (키 종류 무관)

키 노출이 의심되면 아래 순서를 **반드시 순서대로** 따른다. 중간 단계를 건너뛰지 않는다.

1. **확인** — 어떤 키가, 어디서(커밋/로그/화면 공유 등) 노출됐는지 특정한다.
2. **폐기(Revoke)** — 발급처 콘솔에서 해당 키를 즉시 비활성화/삭제한다.
3. **사용 이력 확인** — 폐기 전에 비정상적인 사용 내역(모르는 IP, 과금 급증 등)이 있었는지 확인한다.
4. **재발급(Rotate)** — 새 키를 발급받는다.
5. **환경변수 교체** — `.env` 파일의 값을 새 키로 교체한다. 코드에는 손대지 않는다(하드코딩 금지 원칙).
6. **git 이력 점검** — 만약 키가 커밋에 포함됐다면, 단순 삭제 커밋만으로는 이력에 남는다. 이 경우 되돌릴 수 없는 작업(`filter-branch`, `push --force` 등)이 필요할 수 있으므로 **사용자에게 먼저 확인**한다.

---

## 1. OpenRouter API 키

- **콘솔:** [ ] (예: https://openrouter.ai/keys)
- **폐기 방법:** [ ] 콘솔 로그인 → Keys → 해당 키 Delete
- **사용 이력 확인:** [ ] Activity/Usage 페이지에서 최근 요청 로그 확인
- **재발급:** [ ] Keys → Create Key
- **환경변수 이름:** [ ] (예: `OPENROUTER_API_KEY`)

---

## 2. Oracle 서버 (SSH)

- **SSH 키 위치:** `~/.ssh/oracle-server.key`
- **서버 별칭:** `oracle-server` (SSH config 등록됨)
- **폐기 방법:** [ ] Oracle Cloud 콘솔 → 인스턴스 → SSH 키 교체 또는 인스턴스 재잠금
- **사용 이력 확인:** [ ] `/var/log/auth.log` 또는 콘솔의 로그인 이력에서 낯선 IP 확인
- **재발급:** [ ] 새 SSH 키 쌍 생성 → 콘솔에 공개키 등록 → 기존 키 제거
- **환경변수/설정 교체:** `~/.ssh/config`의 `oracle-server` 항목 및 키 파일 경로 갱신

---

## 3. WordPress

- **관리자 주소:** [ ] (예: https://example.com/wp-admin)
- **폐기 방법:** [ ] 노출된 계정 비밀번호 즉시 변경, Application Password 사용 시 해당 키 Revoke
- **사용 이력 확인:** [ ] 관리자 → 사용자 → 최근 로그인 활동 플러그인/로그 확인
- **재발급:** [ ] 새 비밀번호 또는 새 Application Password 발급
- **환경변수 이름:** [ ] (예: `WORDPRESS_APP_PASSWORD`)

---

## 4. 기타 서비스

새 서비스를 추가할 때는 위와 같은 형식(콘솔/폐기/이력확인/재발급/환경변수)으로 이 문서에 추가한다.

- **서비스명:** [ ]
- **콘솔:** [ ]
- **폐기 방법:** [ ]
- **사용 이력 확인:** [ ]
- **재발급:** [ ]
- **환경변수 이름:** [ ]

---

## 📁 관련 파일

- 환경변수 저장 위치: `~/claude-workspace/.env` (절대 공유·git add 금지)
- 작업 규칙: `~/claude-workspace/CLAUDE.md`
- 작업 기록: `~/claude-workspace/tasks/progress.md` — 키 노출 대응을 했다면 여기에도 기록 남기기

---

*이 문서는 비상 상황용 매뉴얼입니다. 평소에는 CLAUDE.md의 보안 규칙(마스킹, .env 관리 등)을 따르고, 실제 노출 의심 상황에서만 이 절차를 실행하세요.*
